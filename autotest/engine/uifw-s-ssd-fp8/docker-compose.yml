version: "3.9"
services:
  uifw-s-ssd-fp8:
    image: nctu6/uifw-one:air520
    container_name: uifw-s-ssd-fp8-for-autotest
    volumes:
      - /mnt/nvme0:/mnt/nvme0
      - /sys:/sys:ro
      - /etc/machine-id:/etc/machine-id:ro
      - /etc/unieai:/etc/unieai
      - ${MODEL_PATH:-/weights}:/weights
      - ./entrypoint.sh:/entrypoint.sh
    runtime: nvidia
    cap_add:
      - SYS_ADMIN
    network_mode: host
    shm_size: 256gb
    environment:
      NCCL_DEBUG: INFO
      ENGINE: ${ENGINE}
      MODEL_PATH: ${MODEL_PATH:-/weights}
      MODEL_OWNER: ${MODEL_OWNER:-Qwen}
      MODEL_NAME: ${MODEL_NAME:-Qwen3-0.6B}
      TP_SIZE: ${TP_SIZE:-1}
      PORT: ${PORT:-8999}
    ports:
      - "${PORT:-8999}:${PORT:-8999}"
    entrypoint: bash
    command: /entrypoint.sh
    # command: -c "sleep infinity"